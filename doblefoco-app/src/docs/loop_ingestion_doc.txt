====================================================================
ARQUITECTURA DEL LOOP DE INGESTIÃ“N Y NOTIFICACIONES A GMAIL
====================================================================

1. ESTUDIO DE FRECUENCIA DE ACTUALIZACIÃ“N (BENCHMARK INTERNACIONAL)
--------------------------------------------------------------------
- Ground News: Ejecuta loops de scraping y aglomeraciÃ³n con LLMs cada 15 a 30 minutos
  para historias de Ãºltima hora (*Breaking News*).
- AllSides: Ejecuta revisiones de agregaciÃ³n cada 60 minutos.
- Feedly / Google News: Polling constante por Webhooks/RSS cada 10-15 minutos.

Frecuencia recomendada para DobleFoco.co: **Cada 30 Minutos**.
Esta frecuencia balancea el costo de peticiones a la API de LLMs con la frescura
informativa requerida para detectar *Blindspots* en tiempo real.

--------------------------------------------------------------------
2. FLUJO DE TRABAJO TÃ‰CNICO EN PRODUCCIÃ“N
--------------------------------------------------------------------

[Scrapers / RSS Feeds] âž” [LLM Classifier (Gemini/GPT-4)] âž” [Staging Queue (DB)] âž” [Email Alert Worker] âž” [Gmail Moderador]

Paso 1: ProgramaciÃ³n del Cron Job (Backend Node.js / Python Worker)
   - Un script programado (`node-cron` o `Celery`) se activa cada 30 minutos.
   - Extrae los Ãºltimos artÃ­culos publicadas por los 30 medios monitoreados.

Paso 2: ClusterizaciÃ³n y ClasificaciÃ³n de Sesgo por IA
   - Agrupa noticias que traten sobre el mismo evento.
   - Genera el titular neutro, extrae las 3 perspectivas (Izquierda, Centro, Derecha)
     y calcula el Ã­ndice de sesgo ponderado (B_story).

Paso 3: DepÃ³sito en la Zona de ModeraciÃ³n (Staging Area)
   - La historia se inserta con estado `pending` en la base de datos (o LocalStorage en demo).

Paso 4: Disparo de Alerta a Gmail del Moderador
   - Inmediatamente al insertar una o mÃ¡s noticias en Staging, el worker activa una llamada
     a un servicio de mensajerÃ­a transactional (ej. Resend, Nodemailer o Webhook).
   - Formato del correo recibido en Gmail:
     * Asunto: "ðŸš¨ [DobleFoco Staging] 2 Nuevas historias pendientes de moderaciÃ³n"
     * Cuerpo: Resumen rÃ¡pido de los tÃ­tulos capturados + enlace directo al panel `/admin`.

--------------------------------------------------------------------
3. CÃ“DIGO DE REFERENCIA PARA INTEGRACIÃ“N DE EMAIL (NODEJS / RESEND)
--------------------------------------------------------------------

```javascript
// backend/services/notifier.js
import { Resend } from 'resend';
const resend = new Resend(process.env.RESEND_API_KEY);

export async function sendStagingAlert(pendingCount, stories) {
    try {
        await resend.emails.send({
            from: 'alertas@DobleFoco.co',
            to: 'moderador@gmail.com',
            subject: `ðŸš¨ [DobleFoco Staging] ${pendingCount} nueva(s) noticia(s) en la bandeja`,
            html: `
                <h2>Nuevas Noticias Ingeridas por la IA</h2>
                <p>Se han capturado ${pendingCount} historias que requieren tu revisiÃ³n antes de ser publicadas en el feed principal.</p>
                <ul>
                    ${stories.map(s => `<li><strong>${s.title}</strong> (${s.category})</li>`).join('')}
                </ul>
                <a href="https://DobleFoco.co/admin" style="background:#f59e0b;color:#000;padding:10px 20px;text-decoration:none;font-weight:bold;">Ir al Panel de ModeraciÃ³n</a>
            `
        });
        console.log("NotificaciÃ³n enviada exitosamente a Gmail");
    } catch (error) {
        console.error("Error enviando email de notificaciÃ³n:", error);
    }
}
```

